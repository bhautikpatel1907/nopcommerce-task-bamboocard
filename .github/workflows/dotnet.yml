# This workflow will build a NopCommerce v4.80 .NET v9.0-based project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: 
      - dev       # 🔄 Run on feature/active development
      - main      # ✅ Run when PR is merged into main
  #pull_request:
   # branches: 
    #  - main      # 🧪 Run when PR targets main

permissions:
  contents: write # ✅ ADD THIS BLOCK to give the workflow permission to write to releases
  
jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x

    - name: Restore dependencies
      run: dotnet restore src/NopCommerce.sln

    - name: Build
      run: dotnet build src/NopCommerce.sln --configuration Release

    # - name: Run Tests (optional)
    #   run: dotnet test src/NopCommerce.sln
    #   continue-on-error: true

  release-and-deploy:
    needs: build
    runs-on: self-hosted
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Publish Project
        run: dotnet publish src/Presentation/Nop.Web/Nop.Web.csproj -c Release -o published_output
    
    # ------------------------ COMMENTED OUT STEPS ------------------------

    # - name: Zip Published Output
    #   run: zip -r nopcommerce-release.zip published_output/  # 📦 Create ZIP from published output

    # - name: Create GitHub Release
    #   uses: softprops/action-gh-release@v2
    #   with:
    #     tag_name: v${{ github.run_number }}
    #     name: Release ${{ github.run_number }}
    #     body: "Auto release generated from CI"
    #     files: nopcommerce-release.zip
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # 🔐 Auth for creating release

    # ---------------------------------------------------------------------

      - name: Deploy to Local IIS
        run: |
          Write-Host "🧹 Cleaning old deployment..."
          Remove-Item -Recurse -Force "C:\inetpub\wwwroot\bamboo-git*"  # ❌ Remove previous files
          Write-Host "📁 Copying new build to IIS path..."
          Copy-Item -Path "published_output\*" -Destination "C:\inetpub\wwwroot\bamboo-git" -Recurse  # ✅ Deploy new build
